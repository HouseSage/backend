[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
python_classes = Test*

# Add the project root to the Python path
python_paths = .

# Show full diff for failing tests
addopts = -v --tb=short

# Configure test coverage reporting
# To generate coverage report: pytest --cov=app --cov-report=term-missing
# To generate HTML report: pytest --cov=app --cov-report=html
testpaths = tests
python_files = test_*.py
python_functions = test_*
python_classes = Test*
